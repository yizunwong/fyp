# Copy this file to `.env` in the backend folder and fill in values.

# Private key of the first Hardhat account (from `npx hardhat node` output)
PRIVATE_KEY=0x...

# Contract address printed by the deploy script
TRACE_CONTRACT_ADDRESS=0x...

# Local Hardhat JSON-RPC URL
RPC_URL=http://127.0.0.1:8545

