// Re-export for backward compatibility
export {
  useAppLayout as useFarmerLayout,
  useAppLayoutContext as useFarmerLayoutContext,
  AppLayoutProvider as FarmerLayoutProvider,
  type AppLayoutMeta as FarmerLayoutMeta,
  type Breadcrumb as FarmerBreadcrumb,
  type MobileLayoutMeta as FarmerMobileLayoutMeta,
} from "@/components/layout/AppLayoutContext";
